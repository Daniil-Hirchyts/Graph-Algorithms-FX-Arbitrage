{
  "buildCommand": "turbo run build --filter=ui",
  "outputDirectory": "apps/frontend/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
